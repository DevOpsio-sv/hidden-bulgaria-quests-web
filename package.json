{
  "name": "hidden-bulgaria-quests-web",
  "version": "0.1.0",
  "private": true,
  "description": "Official product website for Unlocking Bulgaria, a premium real-world quest app for cultural exploration.",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2026-05-30",
    "deploy": "wrangler pages deploy . --project-name=hidden-bulgaria-quests-web",
    "check": "node --check script.js"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
