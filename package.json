{
  "name": "hidden-bulgaria-quests-web",
  "version": "0.1.0",
  "private": true,
  "description": "Official product website for Unlocking Bulgaria, a premium real-world quest app for cultural exploration.",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-05-30",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=hidden-bulgaria-quests-web",
    "check": "node --check script.js && node --check build.js"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
