# Pre-Astro leftover, kept in sync but NOT deployed.
# `public/_redirects` is the file Astro copies into dist/ and Cloudflare serves.
# This copy is only reachable via the unreferenced legacy build.js.
# Every target below must be an anchor that exists on the homepage.
/ /en 302
/home / 301
/app /#features 302
/download /en#download 302
/partners /en#partners 302
/regions /en#domains 302
